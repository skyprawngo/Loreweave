# 상단 UI와 대용량 원고 스크롤 개선

2026-09-12. 기존 OAuth 변경을 보존하고 UI와 텍스트 엔진을 수정했다.

## 변경

- 이전/다음 → 원형 검색 → 파일 탭을 하나의 macOS 툴바에 배치하고 기본 제목 항목을 제거했다. 검색은 NSSearchField로 펼쳐지며 Return으로 프로젝트 검색, Escape로 축소한다. 탭 선택 시 해당 탭으로 가로 스크롤한다.
- 본문·줄번호·마우스 위치 계산에서 문서 처음부터의 순회를 제거했다. 256행 단위의 정확한 높이 인덱스와 두 단계 이진 탐색을 사용한다. 화면 밖 높이를 추측하지 않아 스크롤 중 전체 높이가 달라지는 문제를 피한다.
- 편집 시 해당 구간의 높이와 직렬화 캐시만 갱신한다. 행 삽입/삭제는 행 번호가 달라지는 뒤쪽 구간을 무효화한다. 전체 문자열도 버전별로 재사용한다.
- 최근 사용한 Core Text 행을 최대 768개 캐시한다. 줄바꿈 결과를 매번 새로 만들지 않는다.
- 외부 변경 확인은 먼저 수정 시각과 크기를 비교한다. 실제 저장 시에는 기존과 동일하게 파일 조정 안에서 디스크 원문을 비교하여 충돌을 막는다.
- 저장은 64KiB UTF-8 버퍼로 같은 볼륨의 임시 파일에 기록·동기화한 후 원자적으로 교체한다. 중간 결과를 원고에 직접 쓰지 않는다.

## 측정

`python3 tests/scroll_performance.py`는 실제 TextlinkTextView/Core Text 코드를 최적화 빌드하여 합성 한글·이모지 원고의 하단을 120번 그린다. `--baseline`은 Git HEAD의 엔진으로 같은 측정을 수행한다. 수치는 오프스크린 본문 draw 호출이며 화면 전체 FPS 또는 트랙패드 지연 측정이 아니다.

| 행 수 | 기존 draw p95 | 수정 draw p95 | 수정 첫 높이 계산 |
|---|---:|---:|---:|
| 1,000 | 3.79ms | 0.36ms | 32ms |
| 10,000 | 33.76ms | 0.32ms | 255ms |
| 100,000 | 334.40ms | 0.50ms | 2,902ms |

세션 복원 후 아직 읽지 않은 탭의 빈 커서 캐시가 디스크 검색을 가리던 문제도 수정했다. 미저장 원고만 캐시를 우선한다.

검색 펼치기 자동 포커스, Escape 축소, 검색창으로 입력어 전달과 제목 제거도 실행 확인했다. `001000` 검색으로 1천·1만 줄 fixture의 1000행 두 결과가 표시되는 것까지 확인했다.

실제 Debug 앱에서 합성 100,001행 원고를 열고 끝에서 98,900행 부근까지 스크롤했으며 본문과 줄번호의 정렬을 확인했다.

## 검증 범위와 남은 비용

- macOS Debug arm64/x86_64 빌드 성공.
- 텍스트 엔진, 문서 binding, 저장·복구 회귀검사 통과. 새 검사는 청크 경계 삽입/삭제, 한글·이모지 직렬화, 폭 변경의 정확한 행 높이 및 UTF-8 저장 버퍼 경계를 포함한다.
- 첫 열기와 폰트·폭 변경은 정확한 전체 줄바꿈 계산이 필요하다. 현재 비동기 초기 레이아웃은 구현하지 않았고 10만 줄에서는 위와 같은 비용이 남는다.
- 문서와 Undo는 메모리에 유지한다. 디스크 기반 rope/piece table로의 전환은 포함하지 않는다. 편집 후 전체 문자열이 필요한 기존 binding 경계와 안전한 전체 파일 교체 비용도 남는다.
- 수정 시각과 크기를 모두 보존한 외부 덮어쓰기는 주기적 UI 감지에서 누락될 수 있지만 저장 전 전체 비교는 유지된다.
- 사용자 원고나 AI 계정의 데이터로 성능 시험하지 않았다.

## 탭 압축·패널 토글 및 UI 일관성 후속 수정

- 가용 너비와 탭 개수에 따라 탭 폭을 줄인다. 활성 탭은 비활성 탭보다 넓으며 기본 비율은 1.4배다. 최소 폭(비활성 64pt, 활성 96pt)에 도달하면 가로 스크롤로 접근성을 유지한다. 새 파일 버튼은 스크롤 영역 밖에 고정했다.
- AI 패널 토글 중 멈춤을 실행 재현하고 프로세스를 샘플링했다. 메인 스레드가 `updateContentSize → ensureLineOrigins → calculateHeight → Core Text`에서 대부분의 시간을 쓰고 있었다. 이 증거는 반복 줄바꿈 계산 병목이며 메모리 누수를 입증하지 않는다.
- 패널 너비 애니메이션을 제거하고 최근 두 너비의 정확한 높이 인덱스를 현재 레이아웃 외에 제한적으로 보관한다. 같은 너비로 돌아오면 재사용하며 편집된 구간은 버전으로 무효화한다. 폭이 넓어질 때 이미 한 줄에 맞는 행은 재측정하지 않는다. 너비 변경 시 현재 읽는 행과 행 안 위치를 유지한다.
- 숨김 시 AI 패널의 뷰·대화 상태를 보존한다. 제목 표시줄과 계정 영역의 겹침을 제거하고 대화 목록 복귀 버튼을 대화 상세 영역으로 이동했다. 파일 이동 버튼의 비활성 상태와 접근성 이름을 실제 이동 가능 여부에 맞췄다.
- 글자 크기·자간은 네이티브 숫자 필드와 Stepper, 줄간격은 Picker로 정리했다. 좁은 창에서는 서식 팝오버로 전환한다. 실제 메뉴가 아닌 글꼴 패널 버튼의 화살표와 잘못된 자간 아이콘을 제거했다.

### 이번 확인 결과

- 최종 macOS Debug 빌드 성공, `git diff --check` 통과.
- `tests/tab_layout_regression.py`: 320/480/800pt에서 2~20개 탭의 압축, 활성 폭, 최소 폭과 가용 너비 경계 검증 통과.
- `tests/text_engine_regression.py`: 기존 검사 및 폭 확대 시 측정 생략, 너비 왕복 캐시 재사용, 편집 후 캐시 무효화 검사 통과.
- `tests/storage_regression.py`: 저장·복구와 UTF-8 스트리밍 저장 검사 통과.
- 실제 Debug 앱에서 합성 100,001행 원고를 연 채 AI 패널 숨김/표시를 두 차례 왕복했다. 멈춤이 재현되지 않았으며 툴바가 넘침 메뉴로 사라지지 않고 대화 상세 선택이 유지됐다. 좁힌 창에서 탭 압축과 서식 팝오버를 확인한 뒤 원래 창 크기로 복원했다.
- 새로운 좁은 너비의 최초 계산 및 초기 열기는 여전히 동기 계산 비용이 있다. 창을 처음 좁히는 실행에서도 지연이 관찰됐다. 이번 결과는 모든 너비의 실시간 리사이즈 성능이나 메모리 누수 부재를 보장하지 않는다. 앞선 draw 측정치는 이번 토글 지연의 측정값이 아니다.

## 상단 창 툴바 후속 정렬

- 기존 반투명 배경을 제목 표시줄까지 확장하고 제목 표시줄 구분선을 제거했다. 본문은 기존 안전 영역을 유지한다.
- GeometryReader 안의 탭 행을 높이 중앙에 배치하여 캡슐과 탐색·검색 버튼의 중심선을 맞췄다.
- 탭 영역의 너비를 에디터 대신 창 너비 기준으로 계산한다. native flexible toolbar spacer 뒤의 primaryAction 그룹에 전체 화면·설정·AI 패널 토글을 배치했다.
- 실제 앱의 AI 패널 표시/숨김 화면을 비교하여 우측 그룹이 같은 위치에 유지되는 것을 확인했다. 탭 폭 또한 패널 토글의 영향을 받지 않는다.
- 설정은 일반 Window(id: settings)로 선언되어 있으므로 상단과 탐색기 설정 버튼 모두 openWindow로 연결했다. openSettings 호출로 열리지 않던 경로를 수정했다.


## 패널 모션·비동기 레이아웃·AI 설정 통합

- AI 패널 토글에 0.25초 smooth 애니메이션, 탭 선택 폭 변화에 0.24초 smooth 애니메이션을 적용했다.
- 패널 드래그는 global 좌표와 드래그 시작 폭으로 계산한다. 최소 280pt, 최대 600pt 및 에디터에 남겨야 하는 폭을 함께 제한하며 종료 시 저장한다.
- 5천 행 이상의 새로운 레이아웃은 취소 가능한 background Core Text 작업으로 준비한다. 연속 크기 변경은 120ms 안정화 이후 측정하며, 문서·버전·스타일이 여전히 일치할 때만 정확한 결과를 설치한다. 준비 중에는 에디터 영역에 진행 표시를 보여준다. 다른 탭과 설정은 조작 가능하다.
- 최근 8개 레이아웃을 보관하고, 한 줄에 들어가던 행의 폭 확대는 기존 높이를 재사용한다. 준비 중 임시 높이를 확정 캐시에 넣지 않는다. 문자·단어 통계도 취소 가능한 백그라운드 작업으로 옮겼다.
- AI 패널의 설정 버튼과 전용 설정 sheet를 제거했다. TextlinkEditor 설정 → AI가 기존 연결·설치·제공자·계정·사용량·로그아웃 옵션을 소유한다. 창 간 인증/요청 소유자는 공유하며 기록 형식과 저장 경로는 변경하지 않았다.

검증: 최종 macOS Debug 빌드 성공. 텍스트 엔진 회귀검사에서 비동기 완료, 빠른 폭 전환 취소, 임시 높이의 잘못된 캐시 승격 방지와 정확한 긴 행 높이를 확인했다. 문서 binding/IME 회귀검사 및 AI fixture 24개 검사도 통과했다. 실제 합성 100,001행 탭 선택·재선택, 패널 양방향 폭 조절·토글, 앱 설정 AI 페이지를 확인했다. 새 폭의 최초 계산에는 진행 표시와 준비 시간이 남으며 모든 크기의 파일이 즉시 열리거나 일정 FPS를 유지한다는 검증은 아니다.
