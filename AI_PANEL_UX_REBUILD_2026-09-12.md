# AI 패널 UX 재구성

2026-09-12. 원고 편집·OAuth·저장 엔진의 기존 변경은 보존하고 AI 화면의 정보 구조를 재구성했다.

| 위치 | 역할과 기능 |
|---|---|
| 패널 상단 | 창작 어시스턴트 제목, 현재 제공자, 이전 대화, 새 대화, AI 설정 |
| 본문 | 선택한 대화의 메시지. 사용자는 약한 배경, AI 답변은 본문 형태로 구별. 시스템 글꼴·텍스트 선택·복사 제공 |
| 하단 입력창 | 새 질문과 후속 질문을 같은 AppKit 입력창에서 작성. Return 전송, Shift+Return 줄바꿈, 응답 중 중지 |
| 입력창의 첨부 | 현재 원고 선택·미리보기, 참조 대화 선택, 전송 범위 설명. 첨부 수를 입력창에 표시 |
| 이전 대화 sheet | 질문 검색, 대화 열기, 개별 삭제, 프로젝트 대화 전체 삭제. 삭제는 확인 단계 제공 |
| AI 설정 sheet | 제공자 변경, 설치·연결 안내, ChatGPT 계정·사용량·로그아웃, 연결 해제 |

화면 전체 높이를 차지하던 대화 카드와 카드 안의 중첩 스크롤, 호버에만 보이던 액션, 카드 순번, 중복 입력 레이아웃, 터미널 프롬프트 장식, 고정폭 대화 글꼴을 제거했다. 기록 목록에서는 대화 요약을 보고, 대화를 선택한 뒤 본문을 읽는다. 메시지 작성 영역은 항상 하단 같은 자리에 있다.

연결 관리 화면은 대화 컨테이너의 ViewModel과 인증 서비스를 공유한다. 별도 서비스 생성이나 OAuth 저장 형식 변경은 없다. 앱 전체 설정 창을 대체한 것은 아니며, 패널의 톱니바퀴에서 열리는 전용 AI 설정 화면이다. 처음 연결하지 않은 상태도 이 설정 화면으로 진입한다.

대화 식별자와 기존 기록 포맷은 유지했다. 새 대화는 nil, 후속 대화는 선택한 conversation ID를 기존 전송 경로에 넘긴다. 작성 중인 입력은 대화 전환 시 지우지 않는다. 원고 첨부는 전송 시점의 활성 원고를 의미하며 미리보기와 전송의 기존 flush 경로를 유지한다.

## 확인

- macOS Debug 빌드 성공, diff 공백 검사 통과.
- 네이티브 입력 회귀검사 7개 통과: 빈 입력 hit area, 폭·줄바꿈, placeholder 클릭 통과, Unicode 입력, 선택 치환, first responder.
- AI fixture 회귀검사 24개 통과: 실행·완료·취소와 기록 계약. fixture의 리소스 누락/미완성 카드 진단은 테스트 로그에 남으며 서비스 실계정 시험과 구분한다.
- 실제 앱에서 이전 대화 목록 → 대화 선택, 첨부 팝오버, 계정·사용량이 있는 AI 설정 → 대화 복귀를 확인했다. 100,001행 합성 원고를 연 상태에서 AI 패널 숨김·표시 후 선택한 대화가 유지됐다.
- 이번 실행 확인에서 실계정 AI 전송·로그아웃·기록 삭제는 하지 않았다. 스트리밍 자동 추적과 파괴적 확인 동작은 추가 장시간 UI 시험이 남는다. 기존 인증과 기록을 그대로 보존했다.
