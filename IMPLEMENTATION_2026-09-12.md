# LoreWeave 검토 결함 수정 결과

작성일: 2026-09-12. 기준: [최초 검토보고서](APP_REVIEW_2026-09-11.md)의 R01–R15. 기존 작업 트리 변경을 유지하며 수정했다. 이 문서는 코드 변경과 검증 결과이며 배포나 실제 사용자 환경의 완료 인증은 아니다. 경쟁 제품에 따른 제품 방향은 [별도 보고서](COMPETITIVE_REVIEW_2026-09-11.md)에 있다.

## 이번에 달라진 사용 흐름

원고를 닫거나 프로젝트를 바꿀 때 저장·버리기·취소를 선택하며, 저장 실패는 작업을 중단한다. 동명 파일 생성은 기존 원고를 덮어쓰지 않는다. 이름 변경과 이동은 열린 탭의 문서 ID와 미저장 내용도 함께 이동한다. 다른 프로그램이 원고를 바꾸면 기준본과 비교해 덮어쓰기를 거부하고 오류를 표시한다.

복구 사본에는 미저장 본문과 기준본을 함께 보관한다. 앱의 Application Support/Loreweave/Recovery에 먼저 쓰고 프로젝트 내부에도 사본을 기록하므로 프로젝트 볼륨에 문제가 생겨도 로컬 사본을 남길 수 있다. 프로젝트가 제공하는 절대 외부 경로는 신뢰하지 않으며, 외부 문서는 앱 소유 사본에서만 복원한다. 손상된 복구 파일은 별도 사본 보존을 시도한다.

편집기는 Unicode·IME 경계를 변환하고 문서별 Undo를 유지한다. 이전 탭에서 늦게 도착한 갱신이 현재 탭이나 새 외부 버전을 덮어쓰지 않도록 문서 ID·URL·내용 revision을 확인한다. 읽기 실패 화면을 빈 원고로 저장하는 경로도 차단했다.

AI 요청은 프로젝트·카드·대화에 귀속된다. 취소된 요청의 늦은 응답은 반영하지 않으며, CLI 로그와 실제 응답을 구조화된 이벤트로 구별한다. 현재 원고 첨부는 명시적으로 선택하며, AI 메뉴는 요청 초안을 준비한다. 실제 전송은 사용자의 전송 버튼에서 이루어진다.

## 검토 항목별 반영

| 항목 | 코드에서 반영한 수정 | 범위와 남은 확인 |
|---|---|---|
| R01 동명 생성 | 신규 파일의 배타적 생성, 파일·폴더 이름 검증, 기존 프로젝트 폴더 덮어쓰기 거부 | 실제 권한·외부 볼륨 환경 확인 필요 |
| R02 닫기·프로젝트 전환 | 미저장 확인, 캐시 flush, 복구 본문 영속화, 세션 없는 프로젝트 전환 초기화 | 비정상 종료 직전 0.5초 이내 입력까지 보장하는 저널은 아님 |
| R03 종료 저장 실패 | 동기 종료 결정에서 저장 실패·취소 시 종료 거부 | OS 강제 종료는 별도 복구 사본에 의존 |
| R04 이동·이름 변경 | 탭 ID 유지, URL·캐시·상태 함께 이동. 삭제는 휴지통으로 이동하고 성공 후 탭 닫기 | 파일 작업 실패 전에 초안을 버리지 않음 |
| R05 외부 충돌 | 저장 직전 기준본 비교, atomic 쓰기, 충돌 오류와 다른 이름 저장 | NSFileCoordinator에 협조하지 않는 외부 작성자와의 원자적 CAS까지 보장하지 않음. 병합 UI는 후속 기능 |
| R06 문자 경계 | Core Text/AppKit UTF-16과 Swift Character 변환, 커서 clamp, 조합·줄바꿈 영역 계산 | 실제 한국어/일본어 입력기 조합과 클릭·스크롤 실측 필요 |
| R07 AI 귀속 | 요청별 프로젝트·카드·대화 소유권, 전환 시 취소·무효화 | 실제 CLI 인증 계정 호출 미실시 |
| R08 취소·종료 | 요청별 timeout, 독립 프로세스 그룹 종료, 취소 후 출력 차단 | 비대화형 Claude/Codex 실행으로 범위 명확화. 기존 터미널 모드는 오류 및 해제 안내 |
| R09 Undo | 문서별 EditorState 유지, 서식·바꾸기 atomic Undo, AI 입력 native Undo | 재실행 후 Undo 스택 복원은 제공하지 않음 |
| R10 기본 명령 | 현재 파일 찾기/바꾸기, 다른 이름 저장, 확대 초기화, 방문 뒤로/앞으로, 프로젝트 본문 검색, 서식, AI 초안 명령 | 사용 경로는 새 프로젝트 검색 화면으로 연결. 이전 Spotlight 실험 코드의 TODO는 미사용 상태로 남음. 검색은 결과 파일을 열고 검색어를 찾으며 동일어가 여러 번 있는 경우 정확한 결과 행 이동은 후속 개선 |
| R11 설정·상태 | 실제 명령이 저장된 단축키 사용, 자동저장 변경 즉시 반영, 미저장·오류·마지막 저장 시각, 미연결 API 키 입력 제거 | 시스템 편집 명령은 native responder 사용. GUI 메뉴와 키 이벤트를 함께 확인할 필요 |
| R12 CLI 응답 | UTF-8 분할 보존, JSONL 구조화 파싱, 종료 코드·인증·버전 오류 구별, 프롬프트 명령 로그 제거 | fixture 검증. 실제 설치 버전·제공자 정책·네트워크에서의 호환성 검증 필요 |
| R13 대화·문맥 | 대화 단위 후속 질문 표시, 전송 시 최신 캐시의 현재 원고 명시 첨부, 첨부 원고 확인 | 선택 문단·여러 참조 파일·설정집 범위 지정은 제품 확장으로 남음 |
| R14 장편 비용 | 줄 높이 누적 캐시, 외부 변경 LCS를 선형 변경 범위로 교체, 디렉터리 지연 로딩·감시, 본문 통계를 내용 변경 시에만 계산 | 실제 1천/1만/10만 행 GUI 성능 실측 미실시. 통계는 여전히 본문 길이에 비례 |
| R15 테마·번역·너비 | 적용 중 테마 기준 통일, ‘나중에 적용’ 의미 수정, 한/영/일 누락 키 보충, AI 너비 드래그·저장 연결 | 실제 색 대비·작은 창·VoiceOver 검증 미실시 |

앱 전체가 singleton 프로젝트·탭 상태를 공유하므로 편집 창을 하나로 제한했다. 다중 프로젝트 창을 제공하려면 상태 소유권을 창별로 분리하는 별도 설계가 필요하다. 읽을 수 없는 최근 프로젝트 기록도 자동 삭제하지 않는다.

## 검증 증거

- `python3 tests/storage_regression.py`: 21개 통과. 실제 Foundation 파일 쓰기와 EditorTabManager를 임시 원고로 실행. 닫기 선택은 NSAlert fixture로 제어한다.
- `python3 tests/text_engine_regression.py`: 37개 통과. 실제 텍스트 엔진·AppKit 입력 객체·Core Text 렌더링 경계 검사.
- `python3 tests/editor_binding_regression.py`: 16개 통과. 실제 Representable 코드를 Context 생성 shim으로 실행하여 문서 전환·지연 갱신을 검사. 실제 SwiftUI 화면 스케줄링 전체를 재현하는 UI 테스트는 아니다.
- `sh tests/ai/run.sh`: 24개 통과. 실제 subprocess를 사용하는 가짜 CLI와 기록 fixture. 실제 AI 호출이나 비용 발생 없음. fixture의 번들 템플릿 미포함·불완전 레거시 카드 진단 로그는 테스트 실패가 아니다.
- `python3 tests/shortcut_regression.py`: 28개 통과. 실제 단축키 매처에서 재지정·비활성화·특수키·별칭·저장 후 복원을 임시 설정 경로로 검사.
- 한/영/일 JSON 각각 380개 키 일치, Swift의 정적 `L10n.get` 참조 누락 없음.
- Debug, generic macOS, 코드 서명 없는 Xcode 빌드 성공. AppIntents 미사용에 따른 메타데이터 추출 경고 1건. 배포 서명·공증 검증 아님.
- `git diff --check` 통과.

전체 실사용 완료를 주장하려면 다음 단계에서 임시 프로젝트로 한글 조합 중 탭 전환·앱 종료, Finder 이름 변경·볼륨 연결 해제, 실제 CLI 인증·취소, 장편 원고 스크롤을 확인해야 한다. 전체 프로젝트 버전 백업·복구 목록·AI 제안 부분 적용은 이번 초안 복구와 별개다.

## 함께 정비한 맥락 문서

저장·편집·파일 시스템·AI·단축키·테마의 `claude.md`와 AGENTS.md를 현재 구현 경계에 맞춰 갱신했다. 과거 검토보고서는 당시 근거로 보존하며, 현재 구현과 혼동하지 않도록 경쟁 보고서에 이번 결과를 연결했다. 기존 사용자 변경을 포함하는 전체 Git diff와 이번 작업의 변경 범위는 같지 않으므로 커밋 시 파일별 검토가 필요하다.
