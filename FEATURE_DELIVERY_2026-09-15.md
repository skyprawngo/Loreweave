# TextlinkEditor 추가 기능 구현·검증

2026-09-15. 최초 앱 검토와 경쟁 제품 조사에서 제안한 핵심 로컬 집필 기능을 구현했다. 기존 네이티브 에디터·상단 UI 변경을 보존한 현재 작업 트리를 기준으로 macOS Debug 빌드와 관련 회귀 검증을 마쳤다. 커밋·배포는 하지 않았다.

## 구현된 기능과 사용 위치

| 제안 | 구현과 진입점 |
|---|---|
| 버전·복구 | 상단 책 아이콘 → 원고 버전. 저장 전 자동 스냅샷, 수동 스냅샷, 날짜별 미리보기, 새 파일로 복원. 문서당 최근 30개를 보관한다. 기존 비정상 종료 초안 복구와 별도다. |
| 프로젝트 일괄 바꾸기 | 검색 → 프로젝트에서 바꾸기. 파일별 원본/결과 미리보기, 선택 적용, 변경 전 버전·작업 기록, 마지막 일괄 변경 되돌리기. 미저장 편집이나 외부 변경이 있으면 적용을 거부한다. |
| AI 수정 비교·부분 수락 | 원고를 첨부한 새 요청의 응답에서 수정 비교. 변경별 선택 후 적용하며 네이티브 Undo를 지원한다. 요청 당시 전체 원고와 현재 내용이 다르면 적용하지 않는다. |
| 명시적 AI 참조 | 입력창 첨부 → AI 참조 자료. 파일, 에디터 우클릭으로 캡처한 선택문, 공개 시점에 맞는 설정 자료를 선택한다. 출처·포함 이유·크기를 보여주며 실제 요청별 최종 프롬프트를 기록한다. |
| 장면 관리 | 책 아이콘 → 집필 자료. 장면 순서, 원고 링크, 시점, 등장인물, 장소, 요약, 진행 상태, 내보내기 포함 여부와 글자 목표. |
| 인물·세계관 자료 | 집필 자료의 설정 자료. 이름·유형·별칭·본문·연결 파일·정보를 아는 인물·공개 시작 장면·AI 포함 여부. 참조 중인 공개 시작 장면은 삭제를 막는다. |
| 수동 AI 검토 | AI 참조 화면에서 일관성, 대사·인물 지식, 제목 검토 요청 준비. 근거와 불확실성을 구분하도록 요청을 구성하고 사용자가 전송한다. |
| 한국어 집필 지표·집중 모드 | 장면별 공백 포함/제외 글자 수, 단어 수와 목표. 책 메뉴의 집중 모드는 탐색·AI 패널·서식 툴바를 숨긴다. |
| 제출·백업 | 장면 순서를 반영한 TXT·Markdown·DOCX 내보내기와 프로젝트 ZIP. 현재 수정 초안도 내보내기에 반영한다. |
| 중복 UI 정리 | 실제 검색 경로에 연결되지 않은 SpotlightView 제거. AI 관련 옵션은 기존 앱 설정 경로를 유지하고 집필 도구를 별도 메뉴에 모았다. |

## 저장과 적용 경계

원고와 집필 메타데이터는 별도로 저장한다. 집필 자료는 프로젝트의 `.{이름}.weavedata/writing-workspace.json`, 버전은 `versions/`, 일괄 바꾸기 저널은 `replacements/`, AI 요청 근거와 수정 기준본은 `ai-context/`와 `ai-revisions/`에 있다. 대화 삭제와 요청 준비 실패 시 연결된 AI 자료도 정리한다. 경로 이탈·심볼릭 링크를 통한 외부 파일 접근을 거부한다.

일괄 바꾸기는 대소문자를 구분하는 일반 문자열 치환이며 최대 200개 원고·파일당 5 MB를 다룬다. 앱 재시작 이후의 자동 일괄 Undo는 제공하지 않는다. 후속 편집이 있으면 되돌리기도 거부하며 버전에서 새 파일로 복원할 수 있다.

AI 참조의 256 KiB 및 최종 요청의 1 MB 제한은 바이트 제한이다. 모델별 토큰 한도를 보장하지 않는다. AI의 설명이나 코드 블록까지 원고로 적용되지 않도록 비교 화면에서 제안을 확인하고 선택해야 한다. 이전 응답에 기준본을 소급 생성하지 않는다.

DOCX는 장면 제목과 일반 문단을 보존하는 내보내기다. Markdown을 출판용 서식으로 변환하거나 정교한 페이지 조판을 수행하지 않는다. 집중 모드는 패널 숨김이며 세션별 순증가 통계나 문단 따라가기 모드는 포함하지 않는다.

## 검증

- macOS Debug `xcodebuild` 성공. 임시 DerivedData의 최신 앱을 실행했다. 배포 서명·공증은 수행하지 않았다.
- `tests/storage_regression.py`, `tests/editor_binding_regression.py`, `tests/native_editor_regression.py` 통과. 문서 귀속, 저장, 네이티브 수정 적용·Undo, 정확한 검색 행 이동 및 합성 대용량 문서를 확인했다.
- `tests/version_history_regression.py`, `tests/writing_workspace_regression.py`, `tests/project_replace_regression.py` 통과. 버전 보존·경로 변경·안전한 복원, 메타데이터·내보내기·DOCX 문자 보존, 일괄 적용·롤백·동시 편집 보호를 확인했다.
- `tests/revision_regression.py`, `tests/ai_context_regression.py`, `tests/ai/run.sh` 통과. 부분 적용·기준본 불일치, 참조 범위·크기 제한, 실제 요청과 동일한 manifest, 실패·삭제 시 자료 정리를 확인했다. AI 실행은 fixture이며 실제 서비스 호출이 아니다.
- 실제 앱의 QA 프로젝트에서 장면·목표·설정 자료 저장, Markdown 내보내기, 수동 버전 생성과 새 파일 복원을 실행했다. 내보낸 본문과 복원 파일의 디스크 바이트를 확인했다.
- 네이티브 에디터의 선택문을 우클릭으로 AI 참조에 추가하고, 설정 자료와 함께 미리보기 및 검토 초안 준비를 확인했다. 외부 AI에 전송하지 않았다.
- 최종 빌드에서 QA 내보내기 파일 한 개의 일괄 바꾸기와 되돌리기를 실행하고, 변경 및 원래 파일 바이트의 정확한 복원을 확인했다.

전체 IME 조합, VoiceOver, 장시간 메모리, 모든 창 크기 및 스크롤 FPS를 전수 입증한 것은 아니다. 실제 구독 OAuth·응답은 이전 검증 기록이며 이번에 재검증하지 않았다.

## 확장 로드맵과 구분

경쟁 조사에서 장기 확장으로 둔 모바일·다중 사용자 협업·무한 캔버스·PDF/EPUB/HWP 조판은 이번 핵심 로컬 집필 범위에 포함하지 않았다. 자동 사실 판정이나 AI 품질 보장도 제공하지 않는다. 이번 완료 범위는 위 표의 실제 사용자 기능과 연결·저장 경로다.
